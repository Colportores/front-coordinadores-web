import { StubPestana } from "@/shell/StubPestana";

// Stub del shell: lo reemplaza la vista de la pestaña Equipo (ver docs/PESTANAS.md).
export default function PestanaEquipo() {
  return <StubPestana pestana="equipo" />;
}
