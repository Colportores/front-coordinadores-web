import { StubPestana } from "@/shell/StubPestana";

// Stub del shell: lo reemplaza la vista de la pestaña Stock (ver docs/PESTANAS.md).
export default function PestanaStock() {
  return <StubPestana pestana="stock" />;
}
