import { StubPestana } from "@/shell/StubPestana";

// Stub del shell: lo reemplaza la vista de la pestaña Cuentas (ver docs/PESTANAS.md).
export default function PestanaCuentas() {
  return <StubPestana pestana="cuentas" />;
}
