import { StubPestana } from "@/shell/StubPestana";

// Stub del shell: lo reemplaza la vista de la pestaña Inicio (ver docs/PESTANAS.md).
export default function PestanaInicio() {
  return <StubPestana pestana="inicio" />;
}
