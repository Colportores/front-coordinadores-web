import { StubPestana } from "@/shell/StubPestana";

// Stub del shell: lo reemplaza la vista de la pestaña Reportes (ver docs/PESTANAS.md).
export default function PestanaReportes() {
  return <StubPestana pestana="reportes" />;
}
